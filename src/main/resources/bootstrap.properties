spring.application.name=microservice-config
spring.profiles.active=dev
server.port=8888

config.server.status.uri=http://microservice-config.service-uri:${server.port}/${spring.application.name}/${spring.profiles.active}
